import { type Config } from 'prettier';

const config: Config = {
    singleQuote: true,
    trailingComma: 'all',
    tabWidth: 4,
    semi: true,
    objectWrap: 'collapse',
};

export default config;
