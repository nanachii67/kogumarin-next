import { Metadata } from 'next';

import CardsLayout from '@/layouts/CardsLayout';

export const metadata: Metadata = { title: 'Socials — Kogumarin' };

export default function SocialCardsPage() {
    return (
        <CardsLayout>
            <></>
        </CardsLayout>
    );
}
